{
  "name": "jev-triage-board",
  "version": "0.1.0",
  "private": true,
  "description": "Kanban board that triages support tickets with TypeSafe AI's Jev. TanStack Start UI, FastAPI backend.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"pnpm dev:api\" \"pnpm dev:web\"",
    "dev:api": "uv run --directory apps/api fastapi dev app/main.py --port 8000",
    "dev:web": "pnpm --filter web dev",
    "test": "pnpm test:api && pnpm test:web",
    "test:api": "uv run --directory apps/api pytest -q",
    "test:web": "pnpm --filter web test",
    "lint": "pnpm lint:api && pnpm lint:web",
    "lint:api": "uv run --directory apps/api ruff check . && uv run --directory apps/api ruff format --check .",
    "lint:web": "pnpm --filter web lint",
    "generate:client": "pnpm --filter web generate:client",
    "export:openapi": "uv run --directory apps/api python -m scripts.export_openapi"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
